services:
  web:
    image: $DOCKER_IMAGE
    build: !reset null
    env_file: !reset null
    environment:
      CI: "true"
    volumes:
      - ./coverage/:/code/coverage/
      - ./ci-share/:/ci-share/
